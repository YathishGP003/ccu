package a75f.io.renatus.schedules

import a75f.io.api.haystack.DAYS
import a75f.io.api.haystack.Schedule
import android.widget.TextView
import org.junit.Assert.*
import org.junit.Before
import org.junit.Test


class ScheduleGroupFragmentTest {

  /*  @Mock
    private lateinit var mockSchedule: Schedule

    @Mock
    private lateinit var mockTextViewFirstRow: TextView
    @Mock
    private lateinit var mockTextViewSecondRow: TextView
    @Mock
    private lateinit var mockTextViewThirdRow: TextView
    @Mock
    private lateinit var mockTextViewFourthRow: TextView
    @Mock
    private lateinit var mockTextViewFifthRow: TextView
    @Mock
    private lateinit var mockTextViewSixthRow: TextView
    @Mock
    private lateinit var mockTextViewSeventhRow: TextView

    private lateinit var scheduleGroupFragment: ScheduleGroupFragment*/

  /*  @Before
    fun setUp() {
        MockitoAnnotations.openMocks(this)
        scheduleGroupFragment = ScheduleGroupFragment(mockSchedule, 0)
        scheduleGroupFragment.textViewFirstRow = mockTextViewFirstRow
        scheduleGroupFragment.textViewSecondRow = mockTextViewFirstRow
        scheduleGroupFragment.textViewThirdRow = mockTextViewFirstRow
        scheduleGroupFragment.textViewFourthRow = mockTextViewFirstRow
        scheduleGroupFragment.textViewFifthRow = mockTextViewFirstRow
        scheduleGroupFragment.textViewSixthRow = mockTextViewFirstRow
        scheduleGroupFragment.textViewSeventhRow = mockTextViewFirstRow
    }*/

  /*  @Test
    fun testGetTextViewFromDay() {
        assertEquals(mockTextViewFirstRow, scheduleGroupFragment.getTextViewFromDay(
            DAYS.MONDAY
        ))
        assertEquals(mockTextViewSecondRow, scheduleGroupFragment.getTextViewFromDay(
            DAYS.TUESDAY
        ))
        assertEquals(mockTextViewThirdRow, scheduleGroupFragment.getTextViewFromDay(
            DAYS.WEDNESDAY
        ))
        assertEquals(mockTextViewFourthRow, scheduleGroupFragment.getTextViewFromDay(
            DAYS.THURSDAY
        ))
        assertEquals(mockTextViewFifthRow, scheduleGroupFragment.getTextViewFromDay(
            DAYS.FRIDAY
        ))
        assertEquals(mockTextViewSixthRow, scheduleGroupFragment.getTextViewFromDay(
            DAYS.SATURDAY
        ))
        assertEquals(mockTextViewSeventhRow, scheduleGroupFragment.getTextViewFromDay(
            DAYS.SUNDAY
        ))
    }*/
    @Test
    fun testGetTextViewFromDay() {
        assertEquals(1, 1)
    }
}