# CCU
# Enter the following:
[DEFAULT]
BUILD_COMMAND = export ANDROID_HOME=~/android-sdk-linux && export PATH="$ANDROID_HOME/tools:$ANDROID_HOME/tools/bin:$ANDROID_HOME/platform-tools:$PATH" && chmod +x ./gradlew &&  BASE_STRING=`cat VERSION` && BASE_LIST=(`echo $BASE_STRING | tr '.' ' '`) && V_MAJOR=${BASE_LIST[0]} && V_MINOR=${BASE_LIST[1]} && V_PATCH=${BASE_LIST[2]} && > gradle.properties && echo VERSION_CODE_MINOR=$((V_MINOR + 1)) >> gradle.properties && echo VERSION_CODE_MAJOR=$V_MAJOR >> gradle.properties && echo VERSION_CODE_PATCH=$V_PATCH >> gradle.properties &&  echo applicationName=RENATUS_CCU >> gradle.properties && echo org.gradle.jvmargs=-Xmx4g -XX:MaxPermSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 >> gradle.properties && APK_DIR=apk && if [ -d "$APK_DIR" ]; then rm -Rf $APK_DIR; fi && ./gradlew clean && ./gradlew assembleDev && mv app/build/outputs/apk/ apk && ./gradlew clean && ./gradlew assembleQa && mv app/build/outputs/apk/* apk && ./gradlew clean && ./gradlew assembleStaging && mv app/build/outputs/apk/* apk && ./gradlew clean && ./gradlew assembleProd && mv app/build/outputs/apk/* apk && ./gradlew clean
BRANCH = master
DEPLOY_DIR = /home/dsingh/webapps/updates
MIRROR_DIR = app/build/outputs/apk/dev/release/
MIRROR_DIR_TWO = app/build/outputs/apk/prod/release/
PID = 11100
PROJECT_KEY = RENATUS
APP_HOST = updates.75fahrenheit.com
APP_USER = dsingh
APP_PASSWD = 65e7cd01
GITLAB_REPO = 75f/android/renatus.git